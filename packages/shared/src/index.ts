// Shared Zod schemas, branded types, and the declaration-matching rule.
//
// The matching rule lives here and nowhere else: every attestor must reach the
// same verdict from the same on-chain state, so it has to be one pure function
// with one set of tests (docs/PLAN.md → "Звірка з декларацією").
export {}
