# tooling manifest — kept on disk for local git hooks, not part of the project
.arena.json

# M0 prototype generated by an external builder — separate toolchain, own
# package.json. Kept on disk for reference, never part of this repository.
m0-prototype/

# secrets
.env
.env.*
!.env.example
*.keypair.json
id.json

# local overrides
*.local

# dependencies
node_modules/
target/

# build output
dist/
build/
*.tsbuildinfo
.turbo/
coverage/

# anchor / solana
.anchor/
test-ledger/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
